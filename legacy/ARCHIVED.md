# Archived gallery

This directory is a self-contained snapshot of the gallery that preceded the
Cloud Run foundation. Nothing under this directory is installed, linted, built,
served, or deployed by the root project.

Individual projects can be moved back into the root application when they are
ready to be revisited. The original package manifest, Yarn lockfile, build
configuration, source, media, and GitHub Pages workflow are retained here for
reference.
